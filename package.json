{
  "name": "ChatkitHelper",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "dependencies": {
    "node-persist": "^2.1.0",
    "pusher-chatkit-server": "^0.9.2",
    "request": "^2.83.0",
    "rxjs": "^5.5.6"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
